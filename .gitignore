# The initial repository snapshot intentionally tracks the complete local
# product tree, including generated projects, build/staging outputs, binaries,
# validation WAVs, logs, and Wwise Profiler captures.
